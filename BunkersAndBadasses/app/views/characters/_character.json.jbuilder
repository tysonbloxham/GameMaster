json.extract! character, :id, :character_name, :level, :experience, :health, :action_points, :badass_points, :evasion, :character_class, :created_at, :updated_at
json.url character_url(character, format: :json)
